namespace Eventual.IntegrationTests.SqlServer
{
    public static class SessionExtensions
    {
        public static void SetupDatabase(this Session session)
        {
            // Run DbUp
            // Tructate the tables
        }
    }
}